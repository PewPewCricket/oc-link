NAME
  ln - create a symlink
  
SYNOPSIS
  ln TARGET           create a symlink
  ln LINK TARGET      create a symlink with symlink at LINK
  ln -t LINK TARGET   create a temporary symlink

DESCRIPTION
  'ln' provides a tool for creating persistant symlinks though an rc script and library.
